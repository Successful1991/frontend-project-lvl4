import { createContext } from 'react';

const authContext = createContext({});
const serviceContext = createContext({});
const filterTextContext = createContext({});

export { authContext, serviceContext, filterTextContext };
